<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

<!-- PROJECT MEMORY — Deepanjan Sen portfolio-2026-v2 -->

# Project memory (keep in mind across sessions)

## What this is
Personal product-design portfolio. Home (`/`) + About (`/about`, dark theme) built with Next.js 16.3.4 (App Router) + React 19 + TypeScript + CSS Modules. No UI/icon/animation/state libraries — native CSS + browser APIs only (Geist/Geist Mono via next/font). GitHub: `github.com/deepanjxn/portfolio-2026-v2`, remote `origin`, branch `master`, Vercel-compatible.

## Key decisions (source of truth — do not break)
- Shared `.container` (globals.css): `width: calc(100% - 2 * var(--page-gutter))`, `max-width: 1392px`, `margin-inline: auto`. Gutter 24px desktop/tablet, 16px mobile (`--page-gutter` override inside the existing `@media (max-width: 767px)` — the only mobile breakpoint, shared by everything).
- Home: intro → "Currently at Layer" 32px; → category controls 120px desktop / 64px mobile (`PortfolioSection .section` margin-top, no new breakpoints). Controls → grid 24px. Category filters [Studies][Visuals][Explainers] always visible on ALL sizes.
- View toggle: hidden entirely on mobile (`ViewToggle.module.css` `@media max-767 .toggle { display: none }`). Desktop + tablet: both icons, interactive (grid 2-col from 768px; single = 1 col). Mobile single-column enforced by grid CSS. Split disabled threshold uses `(max-width: 767px)` matchMedia in ViewToggle; forces `"single"` state on mobile, resumes on tablet.
- Icons: actual SVG assets `public/icons/single-view.svg` + `two-view.svg` (edited to `fill="currentColor"`), rendered via CSS `mask-image` spans colored by button `color` tokens (`--color-icon-active` #8b8b8b / `--color-icon-default` #e0e0e0). No icon library.
- Sticky controls: `.controls` inside `PortfolioSection` is `position: sticky; top: 0; z-index: 10`, with `padding: 24px 0` + negative `margin: -24px 0` (white 24px band above + below while stuck; normal-flow geometry unchanged via margin collapse). Never JS scroll listeners; sticky must anchor to viewport — do NOT re-add `overflow-x: hidden` on `body` (it turns body into a scroll container and kills sticky); root-level clipping lives on `html` only.
- Scrollbar: desktop scrollbars visually hidden, layout space preserved: `html::-webkit-scrollbar { display: none }` under `@media (pointer: fine)`; Firefox via `@supports not selector(...) { scrollbar-width: none }`. `scrollbar-gutter: stable` on html. Never use `overflow: hidden` on body/html for this.
- About: heading→first item 24px, item→item 8px (AboutColumn). Section → section 120px desktop / 64px mobile. Mobile column order Experience → Principles → Tools (AboutColumns 767px media). Intro paragraphs gap 32px, max-width 780px. Footer copyright is `--color-text-primary`.
- About cursor video (`components/about/CursorMedia.*`, data `cursorMediaSource` in `data/about.ts`): 164px wide, auto height (native 1728×2160 4:5), radius 0, fine-pointer/desktop only (CSS + `pointerType === "mouse"`), hidden until ~500ms after mount, then revealed already positioned at the CURRENT cursor (`translate3d` + 24px offset vars), lerp 0.18/frame via rAF in refs (no React state per move), `pointer-events: none`, respects `prefers-reduced-motion` (early return + CSS). Source: `public/videos/deepanjan-sen-about.webm` (VP9, webm MIME).
- Page-load entrance reveal (Home + About): elements marked `data-reveal` (+ `data-reveal="media"` for the slower card variant) animate `opacity 0→1` + `translateY(8px→0)` via pure CSS keyframes gated on a `reveal-entering` class on `<body>` (present in SSR markup, re-added before each client-route paint). Delays are per-element `--reveal-delay` inline vars — stagger 100ms (header 0 → About paragraphs 100/200/300 → columns 400/500/600; Home intro 100, workplace 200, controls 300, media rows 400+); text 800ms, media 950ms, `cubic-bezier(0.22,1,0.36,1)`. `components/layout/PageReveal.tsx` (in root layout) removes the class after 2600ms so re-renders (category/view switches) never replay. Reduced motion disables animations entirely.
- UI sounds (`components/layout/SoundEffects.tsx`, in root layout; assets `public/sfx/click-sound.m4a` + `click-enter.m4a`): delegated `pointerover` plays click-sound once per hover-enter of `a[href], button:not(:disabled), [data-hover-sound]` (project cards), mouse pointer only, no retrigger inside same target; delegated `click` plays click-enter on `a[href], button:not(:disabled)` (category filters + view toggle included). 300ms hover-sound suppression after a click avoids the post-navigation remount ghost. Browsers block audio until the first press: a `pointerdown` anywhere silently unlocks both cached Audio elements (volume 0); volume restored to 1 on real plays.
- Header typography is H3 everywhere (brand "D-S" + About/Email links, `type-h3`), replacing the old type-t1 brand and P1 links.
- Home card radius is 12px (`--radius-card: 12px`, single shared token in globals.css), applied by `.media` (`overflow: hidden`) — every ProjectCard renders the same `.media`, so both views share the radius. The `.media` div also carries `data-radius-surface` (target for FLIP radius compensation).
- Home Split ↔ Single FLIP (`components/projects/ProjectGrid.tsx`, client): pure native FLIP in one `useLayoutEffect` on `[view, projects]`. Rects are DOCUMENT-space (`+ scrollX/Y`) — viewport-space deltas across scroll positions caused the old stepped/L-geometry bug. FIRST = mid-flight live visual rects (clamped to the union of both layouts to reject style-resolution ghosts) or the settled snapshot (kept fresh by a resize listener using transform-aware measurement `layoutRectOf`); LAST = layout rects after clearing inline styles. INVERT per card `translate(dx,dy) scale(sx,sy)` origin `0 0` → recalc → register `transform 450ms cubic-bezier(0.22,1,0.36,1)` → release to none; styles stripped at 480ms. Skipped during entrance reveal and under reduced motion. Cards are matched by `data-project-id`; the same DOM nodes animate (no remount).
- FLIP radius compensation (12px stays visually constant while scaled): while a card is scaled by (sx,sy), a rAF loop (and a synchronous write at inverse time, since the first painted frame precedes the loop) sets the surface's LOCAL radius to `12/sx / 12/sy`; removed again at scale 1. Driven by live computed scale (`readMatrix`), so interrupted restarts stay exact.
- Project-card hover (`ProjectCard.module.css` + `.hoverSurface` wrapper div between the FLIP `article` and `<ProjectMedia/>`): CSS-only `scale(0.98)` (rest 1), 300ms `cubic-bezier(0.22,1,0.36,1)`, origin center, gated `@media (hover: hover) and (pointer: fine)` (off on touch) + `prefers-reduced-motion: reduce` disables it. The wrapper keeps hover transform off the FLIP element (article) and radius surface (.media) so the two systems compose by nesting and never overwrite.
- About mobile column order fix: `AboutColumn` sections apply `columnsStyles[column.id]` from `AboutColumns.module.css` (where the `order` rules live under the 767px media) — previously it referenced non-existent classes in `AboutColumn.module.css`, emitting literal `"undefined undefined"` classes and never reordering (Tools first on mobile). Now mobile = Experience → Principles → Tools; desktop = Tools | Principles | Experience.
- Study Detail pages (`app/studies/[slug]/`, light theme, reuse global Header/Footer): data-driven from `data/studies.ts` — homepage Studies cards render `<Link href="/studies/<slug>">` (ProjectCard takes an optional `href`; card root stays the FLIP element as `a` vs `article`). Every studies-category card slug is prerendered (un-authored ones render a generic placeholder template). Editorial column: fixed/centered 912px on desktop (`width: calc(100% - 2*gutter); max-width: 912px` — NOT fluid, never 1056px; at 1440px side whitespace 264px, at 1920px 504px; header/footer keep the 1392px global container), gutter-fluid below the cap so tablet/mobile inherit the shared container (no new breakpoint). Header: 164px gap desktop / 64px mobile; title→subtitle-row 8px; subtitle (left) + year (right edge, same row, flex space-between), both `type-p1` Text Secondary; mobile stacks year 48px below subtitle AND keeps it right-aligned (base `margin-left: auto` is not reset). Study title (`h1` + `type-h2`) steps down to the H3 tokens 14/20 on mobile only (`.title` rule in the 767px media query). Closing gap mirrors the opening one: `.main` has `margin-bottom: calc(164px - var(--space-120))` (44px) because the global footer reserves 120px top padding — total whitespace after the last block before the footer's first line is 164px. Blocks (text / media / media-cluster) separated 120px desktop / 88px mobile; paragraphs INSIDE one text block 32px (`--space-32`); cluster items exactly 16px (`--space-16`) apart on BOTH axes at every breakpoint — explicit `row-gap`/`column-gap` on `.cluster` (horizontal rows, vertical stacks and wrapped 2-col layouts all 16px; never part of the 120/88 section rhythm). Media panels: full editorial width, 16:10 `aspect-ratio`, radius = the shared `--radius-card` 12px token (same as home cards) applied to ALL panels/embeds globally, `--color-surface-card` fill, `overflow: hidden` + `position: relative`; blank gray placeholders until a block carries media. Panel structure mirrors the homepage hover: fixed `.media` box (geometry/clip only) > `.hoverSurface` (absolute fill; owns the surface fill + radius + `scale(0.98)` hover, 300ms `cubic-bezier(0.22,1,0.36,1)`, center origin, same `(hover: hover) and (pointer: fine)` gating + reduced-motion disable). Every `.media` box also carries `data-hover-sound`, so the global SoundEffects listener plays the homepage's click-sound once per card enter (mouse/fine-pointer only, no retrigger inside a card). The Vimeo embed renders a transparent `.vimeoOverlay` above the iframe — cross-origin iframes never report hover to the parent document, so the overlay captures pointer/hover (and hover sound) over the video; the ambient video needs no pointer input anyway. Real media: `components/studies/StudyMedia` renders when present — `src` (.mp4/.webm/.mov → muted autoplay inline loop video, else `next/image` fill, `object-fit: cover`, sizes `(min-width: 960px) 912px, 100vw`) OR `vimeo` id → Vimeo iframe (`badge=0&autopause=0&app_id=58479&autoplay=1&muted=1&loop=1&controls=0&playsinline=1`, class `.fill`+`.embed`, NO Vimeo player.js) — box geometry identical either way. Study title = `h1` with `type-h2`, body copy `type-p1` text-primary; reveal delays 100/200ms header then 300ms + 100ms per block. LiveStroke (`/studies/livestroke`, homepage Studies card p01) is the only authored case study — 15 blocks in this exact order: Vimeo hero (`1224207606`) → S1 copy (ONE paragraph: "I wanted something that felt polished…") → second Vimeo embed, the Playground (`1224216496`; source-swapped over earlier test ids 1224214199 → 1224215787 → 1224216496 — current id is authoritative) → the "The tricky part…" copy (2 paragraphs) → third visual = `media-cluster` with `layout: "stack"` (the two images VERTICALLY stacked full-width, 16px apart: `livestroke-1.png` code example on top, `livestroke-2.png` rendered result below, both 1200×750 16:10, live in lowercase `/public/studies/livestroke/`) → S3 copy (2 paragraphs) + custom-gradient gray placeholder → `livestroke-3.png` (1586×992; the Playground controls visual, shown ABOVE the bug copy) → "One of the hardest bugs…" copy (2 paragraphs) → S5 copy (2 paragraphs) + gray placeholder → S6 copy (1 paragraph) + 3-item before/after cluster → S7 closing copy (3 paragraphs, the subtle "500 npm downloads" sentence is plain body copy) — the page ENDS on that text, there is NO outro media. Study metadata: title LiveStroke, description "Small React package for animated gradient borders", year 2026. NO headings/captions/labels anywhere — copy-only P1. The S6 default cluster wraps side-by-side; `layout: "stack"` forces single-column full-width items (grid-template-columns: minmax(0,1fr)) — cluster row/column gaps are 16px both axes everywhere, separate from the 120/88 section rhythm. Remaining gray placeholders await real assets in `/public/studies/livestroke/` (a stray unused `livestroke-intro.mp4` sits in the same folder).
- First release committed: `0d02039` "Build About page and refine Home interactions for first release" + `e05b8a7` "Add subtle project card hover interaction"; master tracks `origin/master`; everything after (study page foundation, the LiveStroke case-study content and polish, plus touch-device hover polish across global components) exists as uncommitted working-tree changes — study-related uncommitted files: `app/studies/`, `data/studies.ts`, `components/studies/`, plus edits in `data/projects.ts` (p01 = LiveStroke), `app/studies/[slug]/page.{tsx,module.css}`, `components/layout/SoundEffects.tsx`, `ProjectCard/ProjectGrid`, Header/Footer/CategoryNav/ViewToggle CSS and `AGENTS.md`.
- Assets live under `public/` and are referenced by absolute URL or CSS `url("/...")`; project cards render placeholder panels (no image assets yet); `public/images/.gitkeep` intentionally keeps the future-assets dir alive.

## Workflow
- Use the dev server (`npm run dev`, port 3000) — it is usually already running; do NOT start extra `next start` instances (port conflicts + stale builds caused past debugging confusion).
- Do NOT use `.mjs` scripts for any implementation, automation, testing, or utility task in this project — no new `.mjs` files, no `.mjs` test/validation runs. Prefer the project's existing TypeScript/JavaScript setup and existing tooling (reuse existing files/components/utilities/scripts).
- Do NOT perform automated testing, browser tests, visual regression tests, or unnecessary builds after making changes. The user tests everything manually and reports bugs. Make only the requested changes and stop once implemented. Static checks (`npm run lint`, `npx tsc --noEmit`) may still be run to keep code valid.
- Git: commit style short imperative ("Build …"), no push without explicit request.

